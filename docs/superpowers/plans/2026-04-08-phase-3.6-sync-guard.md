# Phase 3.6 — Sync Guard & Schedule API Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PeerClock.schedule API に同期信頼度ガード・過去時刻契約を導入し、DriftMonitor jump 時に pending event を再スケジュールする。

**Architecture:** PeerClock 内部に lock 配下の最新同期状態 (`lastSyncState`/`lastSyncedAtNs`) を保持し、`currentSync: SyncSnapshot` で同期取得。`schedule` を `throws` 化してガードを順次適用。EventScheduler.handleJump を全 pending event の cancel + 新 now() で再スケジュールに変更。

**Tech Stack:** Swift 6.0 strict concurrency, Swift Testing, Swift Package Manager

**Spec:** `docs/superpowers/specs/2026-04-08-phase-3.6-sync-guard-design.md`

---

## File Structure

- Create: `Sources/PeerClock/SyncSnapshot.swift` — public struct (~50 行)
- Create: `Sources/PeerClock/PeerClockError.swift` — public enum (~25 行)
- Create: `Tests/PeerClockTests/SyncSnapshotTests.swift`
- Create: `Tests/PeerClockTests/PeerClockSyncGuardTests.swift`
- Modify: `Sources/PeerClock/Configuration.swift` — `minSyncQuality`, `syncStaleAfter` 追加
- Modify: `Sources/PeerClock/PeerClock.swift` — `lastSyncState`/`lastSyncedAtNs` 追加、`currentSync`、`schedule` を throws 化、`syncStateForwardTask` で状態保持
- Modify: `Sources/PeerClock/EventScheduler/EventScheduler.swift` — `handleJump` 再スケジュール実装
- Modify: `Tests/PeerClockTests/EventSchedulerTests.swift` — handleJump 再スケジュールテスト 4 件追加
- Modify: `Tests/PeerClockTests/EventSchedulerIntegrationTests.swift` — `try await` 追加 (3 箇所)
- Modify: `Examples/PeerClockDemo/PeerClockDemo/PeerClockViewModel.swift:220` — `try await` 追加 + エラーハンドリング

---

## Task 1: SyncSnapshot struct + tests

**Files:**
- Create: `Sources/PeerClock/SyncSnapshot.swift`
- Create: `Tests/PeerClockTests/SyncSnapshotTests.swift`

- [ ] **Step 1: Create SyncSnapshot.swift**

```swift
import Foundation

/// PeerClock の同期状態のアトミックスナップショット。
///
/// `PeerClock.currentSync` から取得する。`isSynchronized` は同期状態だけでなく
/// 鮮度 (最終同期からの経過時間) も判定する。
public struct SyncSnapshot: Sendable {
    /// 直近の同期状態
    public let state: SyncState
    /// 現在のオフセット (秒、未同期時は 0)
    public let offset: TimeInterval
    /// 直近の品質情報 (未同期時は nil)
    public let quality: SyncQuality?
    /// 直近に .synced へ遷移した時刻 (CLOCK_MONOTONIC ns、未同期時は nil)
    public let lastSyncedAt: UInt64?
    /// このスナップショットを取得した時刻 (CLOCK_MONOTONIC ns)
    public let capturedAt: UInt64
    /// 鮮度判定用の閾値 (Configuration.syncStaleAfter のナノ秒換算)
    private let staleAfterNs: UInt64

    public init(
        state: SyncState,
        offset: TimeInterval,
        quality: SyncQuality?,
        lastSyncedAt: UInt64?,
        capturedAt: UInt64,
        staleAfterNs: UInt64
    ) {
        self.state = state
        self.offset = offset
        self.quality = quality
        self.lastSyncedAt = lastSyncedAt
        self.capturedAt = capturedAt
        self.staleAfterNs = staleAfterNs
    }

    /// 同期済み かつ Configuration.syncStaleAfter 以内に最終同期している。
    /// `capturedAt < lastSyncedAt` の異常時計後退ケースは false 扱い (防御コード)。
    public var isSynchronized: Bool {
        guard case .synced = state, let last = lastSyncedAt else { return false }
        guard capturedAt >= last else { return false }
        return capturedAt - last <= staleAfterNs
    }
}
```

- [ ] **Step 2: Create SyncSnapshotTests.swift**

```swift
import Testing
import Foundation
@testable import PeerClock

@Suite("SyncSnapshot")
struct SyncSnapshotTests {

    private func makeQuality() -> SyncQuality {
        SyncQuality(offsetNs: 0, roundTripDelayNs: 1000, confidence: 1.0)
    }

    @Test("synced + 鮮度内 → isSynchronized true")
    func freshSyncedIsTrue() {
        let snap = SyncSnapshot(
            state: .synced(offset: 0.001, quality: makeQuality()),
            offset: 0.001,
            quality: makeQuality(),
            lastSyncedAt: 1_000_000_000,
            capturedAt: 1_500_000_000,  // +500ms
            staleAfterNs: 1_000_000_000  // 1s
        )
        #expect(snap.isSynchronized == true)
    }

    @Test("境界: 経過時間 == staleAfter ちょうど → true")
    func boundaryEqualStaleAfter() {
        let snap = SyncSnapshot(
            state: .synced(offset: 0, quality: makeQuality()),
            offset: 0,
            quality: makeQuality(),
            lastSyncedAt: 1_000_000_000,
            capturedAt: 2_000_000_000,
            staleAfterNs: 1_000_000_000
        )
        #expect(snap.isSynchronized == true)
    }

    @Test("経過時間 > staleAfter → false")
    func staleAfterExceeded() {
        let snap = SyncSnapshot(
            state: .synced(offset: 0, quality: makeQuality()),
            offset: 0,
            quality: makeQuality(),
            lastSyncedAt: 1_000_000_000,
            capturedAt: 2_000_000_001,
            staleAfterNs: 1_000_000_000
        )
        #expect(snap.isSynchronized == false)
    }

    @Test("state .syncing → false")
    func notSyncedState() {
        let snap = SyncSnapshot(
            state: .syncing,
            offset: 0,
            quality: nil,
            lastSyncedAt: nil,
            capturedAt: 1_000_000_000,
            staleAfterNs: 1_000_000_000
        )
        #expect(snap.isSynchronized == false)
    }

    @Test("state .idle → false")
    func idleState() {
        let snap = SyncSnapshot(
            state: .idle,
            offset: 0,
            quality: nil,
            lastSyncedAt: nil,
            capturedAt: 1_000_000_000,
            staleAfterNs: 1_000_000_000
        )
        #expect(snap.isSynchronized == false)
    }

    @Test("単調時計後退 (capturedAt < lastSyncedAt) → false")
    func monotonicRegression() {
        let snap = SyncSnapshot(
            state: .synced(offset: 0, quality: makeQuality()),
            offset: 0,
            quality: makeQuality(),
            lastSyncedAt: 2_000_000_000,
            capturedAt: 1_000_000_000,  // 後退
            staleAfterNs: 5_000_000_000
        )
        #expect(snap.isSynchronized == false)
    }

    @Test("lastSyncedAt nil → false")
    func nilLastSyncedAt() {
        let snap = SyncSnapshot(
            state: .synced(offset: 0, quality: makeQuality()),
            offset: 0,
            quality: makeQuality(),
            lastSyncedAt: nil,
            capturedAt: 1_000_000_000,
            staleAfterNs: 1_000_000_000
        )
        #expect(snap.isSynchronized == false)
    }
}
```

- [ ] **Step 3: Build & test**

Run: `swift test --filter SyncSnapshotTests`
Expected: 7 tests pass

- [ ] **Step 4: Commit**

```bash
git add Sources/PeerClock/SyncSnapshot.swift Tests/PeerClockTests/SyncSnapshotTests.swift
git commit -m "$(cat <<'EOF'
feat(sync): SyncSnapshot for atomic sync state read

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: PeerClockError enum

**Files:**
- Create: `Sources/PeerClock/PeerClockError.swift`

- [ ] **Step 1: Create PeerClockError.swift**

```swift
import Foundation

/// PeerClock の公開 API がスローするエラー。
public enum PeerClockError: Error, Sendable, Equatable {
    /// PeerClock が start() されていない (eventScheduler == nil)
    case notStarted

    /// 同期されていない or 鮮度が staleAfter を超過している
    case notSynchronized

    /// 同期信頼度が Configuration.minSyncQuality を下回っている
    case qualityBelowThreshold(quality: Double, threshold: Double)

    /// 過去時刻 schedule で遅延が lateTolerance を超過している
    case deadlineExceeded(lateBy: Duration, tolerance: Duration)
}
```

- [ ] **Step 2: Build**

Run: `swift build`
Expected: success

- [ ] **Step 3: Commit**

```bash
git add Sources/PeerClock/PeerClockError.swift
git commit -m "$(cat <<'EOF'
feat(api): add PeerClockError enum for schedule guard

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Configuration fields

**Files:**
- Modify: `Sources/PeerClock/Configuration.swift`

- [ ] **Step 1: Add fields and init parameters**

`syncBackoffPromoteAfter: Int` プロパティの直後 (Clock sync セクション内) に追加:

```swift
    /// schedule() がガードする最小同期信頼度 (0.0..1.0)。
    /// `quality.confidence < minSyncQuality` の場合 `qualityBelowThreshold` を throw。
    /// 比較は厳密未満なので `==` は通過する。
    public let minSyncQuality: Double

    /// SyncSnapshot.isSynchronized が true となる最終同期からの最大経過時間。
    /// デフォルト 90 秒 (Phase 3.5 のバックオフ最大 30s × 2 + マージン)。
    public let syncStaleAfter: Duration

    /// 内部用: syncStaleAfter のナノ秒換算
    internal var syncStaleAfterNs: UInt64 {
        let comps = syncStaleAfter.components
        let secNs = UInt64(max(0, comps.seconds)) * 1_000_000_000
        let attoNs = UInt64(max(0, comps.attoseconds / 1_000_000_000))
        return secNs &+ attoNs
    }
```

init パラメータに追加 (`syncBackoffPromoteAfter: Int = 3,` の直後):

```swift
        minSyncQuality: Double = 0.5,
        syncStaleAfter: Duration = .seconds(90),
```

init 本体に追加 (`self.syncBackoffPromoteAfter = syncBackoffPromoteAfter` の直後):

```swift
        self.minSyncQuality = minSyncQuality
        self.syncStaleAfter = syncStaleAfter
```

- [ ] **Step 2: Build**

Run: `swift build`
Expected: success

- [ ] **Step 3: Commit**

```bash
git add Sources/PeerClock/Configuration.swift
git commit -m "$(cat <<'EOF'
feat(config): add minSyncQuality and syncStaleAfter for sync guard

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: PeerClock — sync state holding + currentSync API

**Files:**
- Modify: `Sources/PeerClock/PeerClock.swift`

- [ ] **Step 1: Add private fields**

`private var driftMonitor: DriftMonitor?` の直後に追加:

```swift
    /// lock 配下に保持される直近の同期状態 (currentSync の元データ)
    private var lastSyncState: SyncState = .idle
    /// lock 配下に保持される直近の .synced 遷移時刻 (CLOCK_MONOTONIC ns)
    private var lastSyncedAtNs: UInt64? = nil
```

- [ ] **Step 2: Add currentSync read API**

`public var coordinatorID: PeerID?` の直後 (Computed セクション内) に追加:

```swift
    /// 現在の同期状態のアトミックスナップショット。
    /// schedule のガードや UI 表示で使用する。
    public var currentSync: SyncSnapshot {
        let captured = NTPSyncEngine.now()
        return lock.withLock {
            let q: SyncQuality? = {
                if case .synced(_, let quality) = self.lastSyncState { return quality }
                return nil
            }()
            let off: TimeInterval = {
                if case .synced(let offset, _) = self.lastSyncState { return offset }
                return 0.0
            }()
            return SyncSnapshot(
                state: self.lastSyncState,
                offset: off,
                quality: q,
                lastSyncedAt: self.lastSyncedAtNs,
                capturedAt: captured,
                staleAfterNs: self.configuration.syncStaleAfterNs
            )
        }
    }
```

- [ ] **Step 3: Update syncStateForwardTask to capture state under lock**

`Sources/PeerClock/PeerClock.swift` line 226-238 付近の `forwardTask` を以下に修正:

現状:
```swift
        let cont = syncStateContinuation
        let forwardTask = Task {
            for await state in eng.syncStateUpdates {
                cont.yield(state)
                if case .synced(let offset, let quality) = state {
                    dm.recordOffset(offset * 1_000_000_000)
                    let offsetNs = Int64(offset * 1_000_000_000)
                    try? await registry.setStatus(offsetNs, forKey: StatusKeys.syncOffset)
                    try? await registry.setStatus(quality, forKey: StatusKeys.syncQuality)
                }
            }
        }
```

変更後:
```swift
        let cont = syncStateContinuation
        let forwardTask = Task { [weak self] in
            for await state in eng.syncStateUpdates {
                cont.yield(state)
                self?.lock.withLock {
                    self?.lastSyncState = state
                    if case .synced = state {
                        self?.lastSyncedAtNs = NTPSyncEngine.now()
                    }
                }
                if case .synced(let offset, let quality) = state {
                    dm.recordOffset(offset * 1_000_000_000)
                    let offsetNs = Int64(offset * 1_000_000_000)
                    try? await registry.setStatus(offsetNs, forKey: StatusKeys.syncOffset)
                    try? await registry.setStatus(quality, forKey: StatusKeys.syncQuality)
                }
            }
        }
```

- [ ] **Step 4: Reset state in stop()**

`Sources/PeerClock/PeerClock.swift` の stop() メソッド内、既存のリセット処理 (knownPeers クリア等) と同じ箇所に追加:

```swift
        lock.withLock {
            self.lastSyncState = .idle
            self.lastSyncedAtNs = nil
        }
```

具体的には PeerClock.swift line 290-300 付近 (`syncStateContinuation.yield(.idle)` の直前) に挿入する。

- [ ] **Step 5: Build**

Run: `swift build`
Expected: success

- [ ] **Step 6: Commit**

```bash
git add Sources/PeerClock/PeerClock.swift
git commit -m "$(cat <<'EOF'
feat(facade): add currentSync read API and lock-protected sync state

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: PeerClock.schedule throws + guards + tests

**Files:**
- Modify: `Sources/PeerClock/PeerClock.swift`
- Create: `Tests/PeerClockTests/PeerClockSyncGuardTests.swift`

- [ ] **Step 1: Replace schedule signature with throws version**

`Sources/PeerClock/PeerClock.swift` line 351-366 付近の現状の `schedule` メソッドを以下に置換:

```swift
    // MARK: - EventScheduler API

    /// 同期済み時刻でアクションを予約する。
    ///
    /// ガード順:
    /// 1. PeerClock 未起動 → `notStarted`
    /// 2. 未同期 or 鮮度切れ → `notSynchronized`
    /// 3. quality < minSyncQuality → `qualityBelowThreshold`
    /// 4. 過去時刻で遅延 > lateTolerance → `deadlineExceeded`
    ///
    /// - parameter atSyncedTime: `clock.now` と同じ時間軸のナノ秒値
    /// - parameter lateTolerance: 過去時刻 schedule で許容する遅延幅。デフォルト 0 (拒否)。
    public func schedule(
        atSyncedTime: UInt64,
        lateTolerance: Duration = .zero,
        _ action: @Sendable @escaping () -> Void
    ) async throws -> ScheduledEventHandle {
        // (0) 未起動チェック
        guard let scheduler = lock.withLock({ eventScheduler }) else {
            throw PeerClockError.notStarted
        }

        // (1) 同期チェック
        let snapshot = self.currentSync
        guard snapshot.isSynchronized else {
            throw PeerClockError.notSynchronized
        }

        // (2) 品質チェック
        if let quality = snapshot.quality {
            let confidence = quality.confidence
            let threshold = configuration.minSyncQuality
            if confidence < threshold {
                throw PeerClockError.qualityBelowThreshold(quality: confidence, threshold: threshold)
            }
        }

        // (3) 過去時刻チェック
        let nowNs = self.now
        if atSyncedTime < nowNs {
            let lateNs = nowNs - atSyncedTime
            let toleranceNs = Self.nanoseconds(from: lateTolerance)
            if lateNs > toleranceNs {
                throw PeerClockError.deadlineExceeded(
                    lateBy: .nanoseconds(Int64(lateNs)),
                    tolerance: lateTolerance
                )
            }
            // tolerance 内 → 即時実行扱いで EventScheduler に渡す
        }

        let id = await scheduler.schedule(atSyncedTime: atSyncedTime, action)
        return ScheduledEventHandle(id: id, scheduler: scheduler)
    }

    /// Duration → ナノ秒変換 (内部用)
    private static func nanoseconds(from duration: Duration) -> UInt64 {
        let comps = duration.components
        let secNs = UInt64(max(0, comps.seconds)) * 1_000_000_000
        let attoNs = UInt64(max(0, comps.attoseconds / 1_000_000_000))
        return secNs &+ attoNs
    }
```

- [ ] **Step 2: Build**

Run: `swift build`
Expected: 既存 schedule 呼び出し側でビルドエラーが出るはず (Task 7 で修正)。
今は **build エラーは無視して進む** (本タスクは facade と新規テストの追加に集中)。
ただし PeerClock.swift 自体のコンパイルは通る必要がある。`swift build 2>&1 | grep "PeerClock.swift"` で PeerClock.swift 起因のエラーがないことを確認。

- [ ] **Step 3: Create PeerClockSyncGuardTests.swift**

```swift
import Testing
import Foundation
@testable import PeerClock

@Suite("PeerClock — Sync Guard")
struct PeerClockSyncGuardTests {

    private func makeConfig(
        minSyncQuality: Double = 0.5,
        syncStaleAfter: Duration = .seconds(90)
    ) -> Configuration {
        Configuration(
            heartbeatInterval: 0.1,
            degradedAfter: 0.5,
            disconnectedAfter: 1.0,
            syncBackoffStages: [0.1],
            syncBackoffPromoteAfter: 1,
            syncMeasurements: 2,
            syncMeasurementInterval: 0.005,
            minSyncQuality: minSyncQuality,
            syncStaleAfter: syncStaleAfter
        )
    }

    @Test("未起動 PeerClock で schedule → notStarted")
    func notStartedThrows() async {
        let network = MockNetwork()
        let clock = PeerClock(
            configuration: makeConfig(),
            transportFactory: { peerID in network.createTransport(for: peerID) }
        )
        // start を呼ばない
        await #expect(throws: PeerClockError.notStarted) {
            _ = try await clock.schedule(atSyncedTime: 1_000_000_000) { }
        }
    }

    @Test("起動済み未同期で schedule → notSynchronized")
    func notSynchronizedThrows() async throws {
        let network = MockNetwork()
        let clock = PeerClock(
            configuration: makeConfig(),
            transportFactory: { peerID in network.createTransport(for: peerID) }
        )
        try await clock.start()
        defer { Task { await clock.stop() } }

        // start 直後は .syncing 状態 (まだ peer 0 で同期未確立)
        await #expect(throws: PeerClockError.notSynchronized) {
            _ = try await clock.schedule(atSyncedTime: clock.now + 1_000_000_000) { }
        }
    }

    @Test("過去時刻 + lateTolerance .zero → deadlineExceeded")
    func deadlineExceededThrows() async throws {
        // 同期完了状態を作るため 2 ピア構成
        let network = MockNetwork()
        let clockA = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        let clockB = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        try await clockA.start()
        try await clockB.start()
        defer {
            Task { await clockA.stop() }
            Task { await clockB.stop() }
        }

        // 同期完了を待機
        try await Task.sleep(for: .milliseconds(500))

        let nowVal = clockA.now
        guard nowVal > 1_000_000_000 else { return } // sanity
        let pastTime = nowVal - 1_000_000_000  // 1秒前

        await #expect(throws: PeerClockError.self) {
            _ = try await clockA.schedule(atSyncedTime: pastTime, lateTolerance: .zero) { }
        }
    }

    @Test("過去時刻だが lateTolerance 内 → 即時実行成功")
    func lateToleranceWithinAccepts() async throws {
        let network = MockNetwork()
        let clockA = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        let clockB = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        try await clockA.start()
        try await clockB.start()
        defer {
            Task { await clockA.stop() }
            Task { await clockB.stop() }
        }
        try await Task.sleep(for: .milliseconds(500))

        let nowVal = clockA.now
        guard nowVal > 50_000_000 else { return }
        let slightlyPast = nowVal - 50_000_000  // 50ms 前

        let fired = MutableBool()
        let handle = try await clockA.schedule(
            atSyncedTime: slightlyPast,
            lateTolerance: .milliseconds(100)
        ) {
            fired.set(true)
        }
        _ = handle
        try await Task.sleep(for: .milliseconds(100))
        #expect(fired.value == true)
    }

    @Test("正常 schedule → 50ms 後に発火")
    func normalScheduleFires() async throws {
        let network = MockNetwork()
        let clockA = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        let clockB = PeerClock(
            configuration: makeConfig(),
            transportFactory: { network.createTransport(for: $0) }
        )
        try await clockA.start()
        try await clockB.start()
        defer {
            Task { await clockA.stop() }
            Task { await clockB.stop() }
        }
        try await Task.sleep(for: .milliseconds(500))

        let fired = MutableBool()
        let target = clockA.now + 50_000_000  // 50ms 先
        _ = try await clockA.schedule(atSyncedTime: target) {
            fired.set(true)
        }
        try await Task.sleep(for: .milliseconds(200))
        #expect(fired.value == true)
    }
}

/// テスト用のスレッドセーフ Bool ホルダー
final class MutableBool: @unchecked Sendable {
    private let lock = NSLock()
    private var _value: Bool = false
    var value: Bool { lock.withLock { _value } }
    func set(_ v: Bool) { lock.withLock { _value = v } }
}
```

- [ ] **Step 4: Build & filter test (other tests will fail to compile until Task 7)**

Run: `swift build 2>&1 | tail -20`
Expected: ビルドエラーは EventSchedulerIntegrationTests.swift / PeerClockViewModel.swift の `try` 不足のみ。
PeerClock.swift 本体・PeerClockSyncGuardTests.swift・SyncSnapshot.swift にエラーがないこと。

このタスクではテスト実行は **後回し** (Task 7 で migration を済ませた後にまとめて実行する)。

- [ ] **Step 5: Commit**

```bash
git add Sources/PeerClock/PeerClock.swift Tests/PeerClockTests/PeerClockSyncGuardTests.swift
git commit -m "$(cat <<'EOF'
feat(facade): make schedule() throws with sync/quality/late guards

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: EventScheduler.handleJump rescheduling + tests

**Files:**
- Modify: `Sources/PeerClock/EventScheduler/EventScheduler.swift`
- Modify: `Tests/PeerClockTests/EventSchedulerTests.swift`

- [ ] **Step 1: Replace handleJump implementation**

`Sources/PeerClock/EventScheduler/EventScheduler.swift` line 93-105 付近の `handleJump` を以下に置換:

```swift
    /// Forwarded by PeerClock when DriftMonitor reports a jump.
    /// 全 pending event について古い Sleeper Task を cancel し、
    /// 新しい now() で delay を再計算して再スケジュールする。
    /// jump で過去化した event は即時 fire (forceMissed) される。
    public func handleJump(oldOffsetNs: Int64, newOffsetNs: Int64) {
        // .pending な event を一括取得 (列挙中の events 変更を避ける)
        let pending = events.filter { $0.value.state == .pending }

        for (id, event) in pending {
            logger.warning(
                "Drift jump rescheduling event \(id.uuidString): old=\(oldOffsetNs) new=\(newOffsetNs)"
            )
            eventContinuation.yield(.driftWarning(
                eventID: id,
                oldOffsetNs: oldOffsetNs,
                newOffsetNs: newOffsetNs
            ))

            // 1. 古い Sleeper Task を cancel (state は .pending のまま)
            event.task?.cancel()
            var updated = event
            updated.task = nil
            events[id] = updated

            // 2. 新しい now() で delay を再計算
            let delay = Int64(updated.atSyncedTime) - Int64(now())
            if delay <= 0 {
                // ジャンプで過去化 → 即時 fire (forceMissed)
                tryFire(id, forceMissed: true)
            } else {
                // 新 Task で再 sleep
                let waitNs = UInt64(delay)
                let task = Task {
                    try? await self.sleeper.sleep(nanoseconds: waitNs)
                    await self.tryFire(id, forceMissed: false)
                }
                updated.task = task
                events[id] = updated
            }
        }
    }
```

- [ ] **Step 2: Add jump rescheduling tests**

`Tests/PeerClockTests/EventSchedulerTests.swift` の既存 @Suite ブロックの最後の `}` の直前に以下を追加:

```swift
    @Test("handleJump: 未来 event を新時間軸で再スケジュール")
    func handleJumpReschedulesToNewTimeline() async throws {
        // FakeSleeper で時刻制御
        let fake = FakeSleeper()
        let nowHolder = NowHolder(initial: 0)
        let scheduler = EventScheduler(
            now: { nowHolder.value },
            sleeper: fake,
            toleranceNs: 1_000_000  // 1ms tolerance
        )

        let fired = MutableInt()
        let id = await scheduler.schedule(atSyncedTime: 1_000_000_000) {
            fired.increment()
        }
        // 500ms 経過
        nowHolder.set(500_000_000)
        // ジャンプ (offset +200ms 相当 → 内部の now() は呼び側が変更済みとして扱う)
        // ここでは now() の差分が delay 再計算に使われることだけ確認
        await scheduler.handleJump(oldOffsetNs: 0, newOffsetNs: 200_000_000)

        // 新 delay = 1_000_000_000 - 500_000_000 = 500_000_000
        // FakeSleeper でその時間進めると fire するはず
        try await fake.advance(to: 1_000_000_000)
        try await Task.sleep(for: .milliseconds(50))  // detached action 完了待ち

        let state = await scheduler.state(of: id)
        #expect(state == .fired || state == .missed)
        #expect(fired.value == 1)  // 二重実行されていない
    }

    @Test("handleJump: 過去化した event は即時 fire (missed)")
    func handleJumpFiresPastEventImmediately() async throws {
        let fake = FakeSleeper()
        let nowHolder = NowHolder(initial: 0)
        let scheduler = EventScheduler(
            now: { nowHolder.value },
            sleeper: fake,
            toleranceNs: 1_000_000
        )

        let fired = MutableInt()
        let id = await scheduler.schedule(atSyncedTime: 1_000_000_000) {
            fired.increment()
        }
        // 大きく前進 (jump で 2 秒先になった想定)
        nowHolder.set(2_000_000_000)
        await scheduler.handleJump(oldOffsetNs: 0, newOffsetNs: 2_000_000_000)

        try await Task.sleep(for: .milliseconds(50))
        let state = await scheduler.state(of: id)
        #expect(state == .missed)
        #expect(fired.value == 1)
    }

    @Test("handleJump: 複数 pending を全件再照準")
    func handleJumpReschedulesAllPending() async throws {
        let fake = FakeSleeper()
        let nowHolder = NowHolder(initial: 0)
        let scheduler = EventScheduler(
            now: { nowHolder.value },
            sleeper: fake,
            toleranceNs: 1_000_000
        )

        let firedA = MutableInt()
        let firedB = MutableInt()
        let firedC = MutableInt()
        _ = await scheduler.schedule(atSyncedTime: 1_000_000_000) { firedA.increment() }
        _ = await scheduler.schedule(atSyncedTime: 2_000_000_000) { firedB.increment() }
        _ = await scheduler.schedule(atSyncedTime: 3_000_000_000) { firedC.increment() }

        nowHolder.set(500_000_000)
        await scheduler.handleJump(oldOffsetNs: 0, newOffsetNs: 100_000_000)

        // 全 3 件が新 Task で再スケジュールされたか: 各 target まで進めて確認
        try await fake.advance(to: 1_000_000_000)
        try await Task.sleep(for: .milliseconds(20))
        try await fake.advance(to: 2_000_000_000)
        try await Task.sleep(for: .milliseconds(20))
        try await fake.advance(to: 3_000_000_000)
        try await Task.sleep(for: .milliseconds(20))

        #expect(firedA.value == 1)
        #expect(firedB.value == 1)
        #expect(firedC.value == 1)
    }

    @Test("handleJump: 二重実行されない")
    func handleJumpNoDoubleFire() async throws {
        let fake = FakeSleeper()
        let nowHolder = NowHolder(initial: 0)
        let scheduler = EventScheduler(
            now: { nowHolder.value },
            sleeper: fake,
            toleranceNs: 1_000_000
        )

        let fired = MutableInt()
        _ = await scheduler.schedule(atSyncedTime: 1_000_000_000) {
            fired.increment()
        }
        // ジャンプを 2 回連続発生させる
        nowHolder.set(500_000_000)
        await scheduler.handleJump(oldOffsetNs: 0, newOffsetNs: 100_000_000)
        await scheduler.handleJump(oldOffsetNs: 100_000_000, newOffsetNs: 200_000_000)

        try await fake.advance(to: 1_000_000_000)
        try await Task.sleep(for: .milliseconds(50))

        #expect(fired.value == 1)  // 1 回だけ
    }
```

注: 上記テストは `FakeSleeper`、`NowHolder`、`MutableInt` ヘルパに依存する。EventSchedulerTests.swift に既存ヘルパがあれば再利用。なければ Step 3 で追加する。

- [ ] **Step 3: Add test helpers if missing**

`Tests/PeerClockTests/EventSchedulerTests.swift` の冒頭付近で既存ヘルパを確認:

Run: `grep -n "FakeSleeper\|NowHolder\|MutableInt" Tests/PeerClockTests/EventSchedulerTests.swift`

`FakeSleeper` と `NowHolder` は既存実装があるはず (現在のテストが時刻制御している)。`MutableInt` がない場合は @Suite の外側に追加:

```swift
final class MutableInt: @unchecked Sendable {
    private let lock = NSLock()
    private var _value: Int = 0
    var value: Int { lock.withLock { _value } }
    func increment() { lock.withLock { _value += 1 } }
}
```

既存ヘルパの signature が異なる場合は、テストを既存スタイルに合わせて書き直す。

- [ ] **Step 4: Run targeted tests**

Run: `swift test --filter EventSchedulerTests`
Expected: 既存テスト + 新規 4 件すべて pass。
ビルドエラーが他ファイル (Integration tests / Demo) で出ても、`--filter` で対象を絞れば EventSchedulerTests 自体は実行できる。
失敗する場合は FakeSleeper の API を確認して合わせる。

- [ ] **Step 5: Commit**

```bash
git add Sources/PeerClock/EventScheduler/EventScheduler.swift Tests/PeerClockTests/EventSchedulerTests.swift
git commit -m "$(cat <<'EOF'
feat(scheduler): reschedule pending events on drift jump

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Migration — try await for existing schedule callers

**Files:**
- Modify: `Tests/PeerClockTests/EventSchedulerIntegrationTests.swift`
- Modify: `Examples/PeerClockDemo/PeerClockDemo/PeerClockViewModel.swift`

- [ ] **Step 1: Update EventSchedulerIntegrationTests.swift**

3 箇所を `try await` に変更:

Line 27 付近:
```swift
        let handle = try await clock.schedule(atSyncedTime: when) {
```

Line 50 付近:
```swift
        let handle = try await clock.schedule(atSyncedTime: when) {
```

Line 89 付近:
```swift
        _ = try await a.schedule(atSyncedTime: target) {
```

Line 93 付近:
```swift
        _ = try await b.schedule(atSyncedTime: target) {
```

各テスト関数のシグネチャに `throws` を追加 (既存に `async throws` がなければ)。
必要に応じて `try` を伴う schedule 呼び出しの周囲を do-catch で囲むか、テスト関数全体を `async throws` にする。

Run: `grep -n "func.*async" Tests/PeerClockTests/EventSchedulerIntegrationTests.swift` で既存シグネチャ確認。

- [ ] **Step 2: Update PeerClockViewModel.swift**

`Examples/PeerClockDemo/PeerClockDemo/PeerClockViewModel.swift:220` 付近:

現状想定:
```swift
        let handle = await clock.schedule(atSyncedTime: target) { [weak self] in
            ...
        }
```

変更後:
```swift
        do {
            let handle = try await clock.schedule(atSyncedTime: target) { [weak self] in
                ...
            }
            // 既存の handle 後処理
        } catch let error as PeerClockError {
            // エラー表示用に状態に格納 (既存パターンに合わせる)
            self.lastScheduleError = "\(error)"
        } catch {
            self.lastScheduleError = "\(error)"
        }
```

`lastScheduleError` プロパティが存在しない場合は、ファイル内に既存のエラー表示プロパティ (e.g., `errorMessage`) があればそれを使う。なければシンプルに print + 既存処理スキップで OK:

```swift
        do {
            let handle = try await clock.schedule(atSyncedTime: target) { [weak self] in
                ...
            }
            // 既存の処理
        } catch {
            print("schedule failed: \(error)")
        }
```

実装時に PeerClockViewModel.swift の line 215-235 を Read してから既存スタイルに合わせて最小修正する。

- [ ] **Step 3: Build**

Run: `swift build`
Expected: success (警告ゼロ)

Xcode プロジェクトのビルドは Task 8 で確認。

- [ ] **Step 4: Run full test suite**

Run: `swift test`
Expected: **全テスト pass** (Task 1-6 で追加した新規テスト含め全部緑)

- [ ] **Step 5: Commit**

```bash
git add Tests/PeerClockTests/EventSchedulerIntegrationTests.swift Examples/PeerClockDemo/PeerClockDemo/PeerClockViewModel.swift
git commit -m "$(cat <<'EOF'
chore: migrate existing schedule() callers to try await

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Final verification

- [ ] **Step 1: Full build & test**

Run: `swift build && swift test`
Expected: 警告ゼロ、全テスト pass

- [ ] **Step 2: Verify no stale references**

Run: `grep -rn "\.schedule(atSyncedTime" Sources/ Tests/ Examples/ | grep -v "try await" | grep -v "scheduler.schedule"`
Expected: 出力ゼロ (PeerClock facade 経由はすべて try await、EventScheduler 直接呼びは scheduler.schedule の形)

もし stale な呼び出しが残っていれば修正してコミット追加。

- [ ] **Step 3: Inspect commit history**

```bash
git log --oneline main..HEAD
```

Phase 3.6 の全コミットが揃っていることを目視確認 (8 task × 1 commit = 8 commits)。

---

## Self-Review Checklist

- [x] Spec coverage:
  - SyncSnapshot: Task 1
  - PeerClockError: Task 2
  - Configuration fields: Task 3
  - PeerClock currentSync + sync state holding + stop reset: Task 4
  - schedule throws + 4 ガード: Task 5
  - PeerClockSyncGuardTests (notStarted, notSynchronized, deadlineExceeded, lateTolerance 内, 正常): Task 5
  - EventScheduler.handleJump 再スケジュール: Task 6
  - EventSchedulerJumpTests (未来再照準, 過去化, 複数 pending, 二重実行防止): Task 6
  - 既存呼び出し側 migration: Task 7
- [x] Placeholder scan: 全ステップに actual code か明確なコマンドあり
- [x] Type consistency: `SyncSnapshot`, `PeerClockError`, `currentSync`, `lastSyncState`, `lastSyncedAtNs`, `minSyncQuality`, `syncStaleAfter`, `syncStaleAfterNs`, `lateTolerance` が全タスクで同名
- [x] TDD: Task 1/2 でテストと型を同時、Task 5/6 でガード/再スケジュール実装と直後にテスト
- [x] Frequent commits: 8 tasks × 1 commit each

## Known Risks

- **Task 5 のテスト**: PeerClockSyncGuardTests は MockNetwork で 2 ピア構成し同期を待機する。`Task.sleep(for: .milliseconds(500))` で同期完了を期待しているが、CI 環境やマシン負荷で 500ms では足りない可能性あり。失敗したら 1000ms に増やす。
- **Task 6 の FakeSleeper API 整合**: 既存 EventSchedulerTests のヘルパに依存。signature が想定と異なれば実装時に既存スタイルに合わせる。
- **Task 7 の Demo Xcode ビルド**: `swift build` (SwiftPM) で通っても Xcode プロジェクトは別途 build が必要。実機テストは plan スコープ外。
- **`Configuration.syncStaleAfterNs` の internal 公開**: SwiftPM の同モジュール内テストからは見える。`@testable import` を SyncSnapshotTests で使うので問題なし。
