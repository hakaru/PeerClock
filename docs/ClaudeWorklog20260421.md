# Claude Worklog 2026-04-21


## 00:27 - 作業ログ（自動記録）
- App Store の情報をもう少し詳しく見ます。

## 00:30 - 作業ログ（自動記録）
- ## Prime Time NTP 調査結果 ### 基本情報 | 項目 | 内容 | |------|------| | **名称** | Prime Time NTP | | **App Store** | [apps.apple.com/us/app/prime-time-ntp/id6759555624](https://apps.apple.com/us/app/prime-time-ntp/id6759555624) | | **開発者** | Kentaro Saito | | **価格** | 無料（Pro は買い切り IAP） | | **プラットフォーム** | iPad（iPhone 兼用かは不明） | | **プライバシー** | データ収集なし | ### 機能 **無料版:** - NTP サーバー最大 5 台にリアルタイム同期 - ミリ秒精度表示（120fps） - ネットワークオフセット & RTT モニタリング - オフセット/RTT のスパークライングラフ - Stratum & Reference ID 表示 **Pro（買い切り）:** - 手動同期（オンデマンド） - Always-on display - NTP サーバー最大 30 台同時監視 ### ターゲット > developers, network engineers, or anyone curious about the true accuracy of their device's clock --- ### PeerClock 戦略への示唆 | 観点 | 分析 | |------|------| | **直接競合か** | **No**。Prime Time NTP は純粋な NTP クライアント（サーバー→デバイス）。PeerClock のデバイス間 P2P とは別レイヤー | | **市場の存在証明** | 「NTP の中身を可視化する」だけでアプリとして成立している。開発者/ネットワークエンジニア向けニッチには需要あり | | **UI/UX 参考** | ミリ秒 120fps 表示、スパークライングラフ、RTT 可視化 ← **PeerClock Demo アプリの参考になる** | | **差別化ポイント** | Prime Time NTP は **1台で完結**。PeerClock Demo は**2台以上並べて同期の魔法を見せる** ← ここが明確な差 | ### もし PeerClock でこの方向を取るなら Prime Time NTP のような「NTP 可視化」を**ベースレイヤー**にし、そこに**ピア間同期を重ねて表示**すると差別化が明確: ``` 画面上部: ミリ秒時計（120fps）

## 00:32 - 作業ログ（自動記録）
- 理解しました。方向が定まった。 ## 確定した戦略 **NTP を入口（わかりやすいキーワード）、P2P Sync を「魔法の瞬間」にする説明アプリ** | 要素 | 役割 | |------|------| | NTP 可視化 | フック。「正確な時刻」は誰でも理解できる | | Peer Sync | 差別化。2台並べた瞬間に「これ自作では無理」と伝わる | | 課金 | 不要。PeerClock ライブラリの営業資料 | | 名前 | NTP を含む（検索性） + Peer/Sync を含む（差別化） | ## アプリの本質（1文で） > 「1台なら高精度NTP時計。2台並べたらインターネット不要のミリ秒同期が走り、PeerClock の魔法が可視化される」 ## 最小構成（MVP） **1台モード（NTP クライアント）:** - ミリ秒時計 120fps - NTP オフセット & RTT 表示 - → 単独でも実用的（Prime Time NTP 相当） **2台モード（PeerClock デモ）— 自動遷移:** - 近傍ピア発見で自動接続（Bonjour） - 「Synced with Peer」表示 - **ピア間オフセットが リアルタイムで 0.x ms と表示される** - 画面タップ → 両端末同時反応（色変化 or フラッシュ） - → これが Prime Time NTP には**絶対にできない**こと ## 次のアクション `tasks/todo.md` を**精度向上計画からこのアプリ計画に書き換える**形で進めますか？ 具体的には: 1. アプリ名候補を決める（例: PeerNTP / SyncTime / PeerClock NTP） 2. 画面設計（1画面のみ、設定なし） 3. 実装計画（PeerClock ライブラリ + SwiftUI、1〜2週間） 4. TestFlight 公開準備 進めますか？

## 00:33 - 作業ログ（自動記録）
- これは非常に良い差別化軸。 ## なぜ良いか | シナリオ | Prime Time NTP | このアプリ | |----------|---------------|-----------| | インターネットあり | ✅ NTP で正確 | ✅ NTP + P2P | | **インターネットなし**（機内、野外、地下） | ❌ 動かない | **✅ P2P で相対同期が動く** | | NTP サーバー選択 | 手動設定 | **自動選択（最低RTT）** | **「NTP なしでも動く」= PeerClock の存在意義そのもの**を自然に体験させられる。 ## プロダクトストーリー > 1. アプリ起動 → NTP サーバーに自動接続（最適サーバー自動選択）→ 正確な時刻表示 > 2. インターネットが切れる → **「Offline — Peer Sync Active」** と表示が変わる → でもミリ秒時計は動き続ける > 3. 2 台を近づける → **ローカル P2P で同期継続**、相対精度 ±2ms を維持 > 4. → 「NTP がなくても、隣のデバイスと時間が揃う」が一目でわかる ## 自動サーバー選択の実装イメージ ``` 起動時: 1. 候補プール（time.apple.com, ntp.nict.jp, pool.ntp.org 等）に並列 ping 2. RTT 最小のサーバーを自動選択 3. バックグラウンドで定期的に再評価（ネットワーク変更時） Offline fallback: - NTP 不達 → DriftMonitor で最後の NTP offset を基に推定 - P2P ピアがいれば相対同期で補正 - 単独＋NTP不達 → 「±Xms estimated」と不確実性を表示 ``` ## 機能マトリクス | 状態 | 時刻ソース | 精度表示 | |------|-----------|---------| | NTP + Peer | NTP 絶対時刻 + P2P 相対同期 | ±0.5ms | | NTP のみ（1台） | NTP 絶対時刻 | ±数ms（ネットワーク依存） | | **Peer のみ（Offline）** | **P2P 相対同期** | **±2ms（PeerClock の本領）** | | 単独 + Offline | デバイスクロック + ドリフト推定 | ±数十ms〜（不確実性を明示） | ## これで固まる差別化

## 00:35 - 作業ログ（自動記録）
- 決定。**PeerClock NTP** で行きましょう。 `tasks/todo.md` を精度向上計画からアプリ計画に書き換えます。

## 00:36 - 作業ログ（自動記録）
- `tasks/todo.md` をアプリ計画に書き換えました。 ## PeerClock NTP — 計画サマリー | Phase | 内容 | 期間 | |-------|------|------| | 1 | NTP 単体モード（自動サーバー選択、ミリ秒時計、RTT表示） | 3日 | | 2 | Peer Sync 統合（Bonjour 発見、Offline P2P、TAP SYNC） | 3日 | | 3 | ポリッシュ & TestFlight 配信 | 2日 | | 4 | 公開 & マーケ（README動画、SNS、SPI） | 2日 | **合計: 約10日** ### 最初の判断ポイント NTP クライアント部分を: - **A**: PeerClock の `NTPSyncEngine` を外部サーバー向けに拡張（一貫性） - **B**: Kronos を別途依存に追加（即使える、実績あり） - **C**: 自前で軽量 SNTP 実装（依存ゼロ維持） どれにするかで Phase 1 の進め方が変わります。どうしますか？

## 00:38 - 作業ログ（自動記録）
- ## Kronos | 項目 | 内容 | |------|------| | **URL** | [github.com/MobileNativeFoundation/Kronos](https://github.com/MobileNativeFoundation/Kronos) | | **元開発** | Lyft → 現在 MobileNativeFoundation 管理 | | **Stars** | 692 | | **最終更新** | 2025-06-12（現役メンテ） | | **言語** | Swift | | **対応** | iOS 12+, macOS 13+, tvOS 12+ | | **配布** | SPM / CocoaPods | ### 何をするライブラリか ```swift import Kronos // 1. NTP サーバーと同期（デフォルト: time.apple.com） Clock.sync() // 2. 正確な現在時刻を取得 let trueNow = Clock.now // Date?（同期完了後に non-nil） ``` **これだけ。** デバイスの時計が手動で変更されていても、NTP サーバーから取得した「真の時刻」を返す。 ### 仕組み 1. NTP プール（time.apple.com）の最大 5 サーバーに並列リクエスト 2. 最初のレスポンスで即座にコールバック → その後もっと精度の高い値に更新し続ける 3. `mach_absolute_time` ベースのモノトニッククロックで **デバイス時刻変更に影響されない** 4. オフセットを `UserDefaults` にキャッシュ → 次回起動時に即座に暫定値を返せる ### PeerClock NTP での使い方（案B） ```swift import Kronos import PeerClock // NTP（インターネット経由の絶対時刻） Clock.sync(from: "ntp.nict.jp") { date, offset in // offset = デバイス時計との差（秒） updateNTPDisplay(offset: offset) } // P2P（ローカル同期） let peerClock = PeerClock(configuration: .default) // → Offline でもピア間 ±2ms 同期 ``` ### Kronos の長所 / 短所 | 長所 | 短所 | |------|------| | 枯れた実装、Lyft 本番使用実績 | NTP サーバー向け**のみ**（P2P なし） | | SPM 対応、導入 1 行 | Swift 6 Strict Concurrency 未対応 | | モノトニッククロック対応 | コールバック API（async/await 非対応） | | オフセッ

## 00:46 - 作業ログ（自動記録）
- 計画を更新しました。レビューに回します。

## 00:49 - 作業ログ（自動記録）
- 回答待ちです。C案（Kronos 不要、PeerClock の NTPSyncEngine を外部 NTP サーバー対応に拡張）で進めますか？

## 00:57 - 作業ログ（自動記録）
- 更新完了。最終状態を確認します。

## 06:31 - 作業ログ（自動記録）
- Gemini レビュー完了。結果を確認します。

## 06:33 - 作業ログ（自動記録）
- PrivacyInfo.xcprivacy について — Phase 1 は `Date()` のみ使用（`mach_continuous_time` は直接呼ばない）なので、Step 0 から除外します。

## 11:13 - 作業ログ（自動記録）
- 全て untracked ファイル。Gemini レビュー最終ラウンドを実行しつつ、差分を確認します。

## 13:20 - 作業ログ（自動記録）
- 動作確認ありがとうございます。まず TAP SYNC の変更をコミットします。

## 13:22 - 作業ログ（自動記録）
- プッシュ完了。PeerClock を使った新しいアプリのアイデアです： **実用系:** 1. **マルチカメラ同期シャッター** — 複数 iPhone で同時撮影。結婚式やイベントで複数アングルを完全同期 2. **音楽メトロノーム同期** — バンドリハーサル用。全員のメトロノームが ±2ms で揃う 3. **分散タイマー** — 料理教室・ワークショップで全員のタイマーが同時にスタート/アラーム **エンタメ系:** 4. **ライトショー** — コンサート会場で観客のスマホが同期して光る（色パターン配信） 5. **同期ゲーム** — 「同時にタップ」の精度を競う対戦ゲーム **プロ向け:** 6. **マルチデバイス録音** (1Take の一般化) — ポッドキャスト複数マイク同期 7. **スポーツ計測** — スタート/ゴールに置いた 2 台で正確なタイム計測 どの方向が面白そうですか？ あるいは別のアイデアがあれば。

## 13:24 - 作業ログ（自動記録）
- メトロノームいいですね。PeerClock の強みが直感的に伝わる。 **コンセプト**: 2台以上の iPhone を並べて同じ BPM で鳴らす。±2ms の同期精度なら人間の耳では完全に同時に聞こえる（知覚閾値 ~20ms）。 **差別化**: 既存メトロノームアプリは単独動作。「バンドメンバー全員のメトロノームが揃う」は存在しない。 **デモの強さ**: 2台並べてタップ → 同時にクリック音が鳴る → 「ズレてない！」が音で体感できる。動画映えする。 次のステップとして、もう少し掘り下げますか？（機能スコープ、UIイメージ、技術的な課題の洗い出しなど）

## 13:29 - 作業ログ（自動記録）
- Plan エージェントが既存ファイルに追記していないようです。計画を直接書きます。

## 13:30 - 作業ログ（自動記録）
- 計画完成しました。要点： **Phase 1**（単体メトロノーム）: 6 Step、約 4 時間 - ClickSynthesizer: AVAudioEngine でサイン波クリック 3 種生成 - MetronomeEngine: `mach_absolute_time` ベースの精密スケジューラ（Task.sleep は精度不足） - 1 画面 UI: BPM 表示 + サブディビジョン選択 + 背景フラッシュ **Phase 2**（P2P 同期）: 3 Step、約 2 時間 - `PeerClock.now % beatIntervalNs` で全端末が独立して同じ拍を打つ - BPM/subdivision 変更は CommandRouter.broadcast で即時配信 **最大のリスク**: タイミング精度。`Task.sleep` では ±10ms ジッター → `AVAudioPlayerNode.scheduleBuffer(at:)` で音声再生時刻を直接指定する方式が最も安全。 進めますか？

## 13:31 - 作業ログ（自動記録）
- Codex と Gemini に並列でレビュー依頼中です。完了通知を待っています。

## 13:33 - 作業ログ（自動記録）
- 両レビュー完了。指摘を統合します。 ## レビュー結果統合 | 優先度 | 指摘 | 両者一致 | 対応 | |--------|------|----------|------| | **High** | BPM 変更時の切替点を絶対時刻 `applyAtNs` で broadcast (#8) | ✅ | 計画に反映 | | **High** | デバイス間 `outputLatency` 差の補正 (#9) | Gemini | 計画に反映 | | **Medium** | `scheduleBuffer(at:)` + ルックアヘッド（2-3拍先スケジュール）で busy-wait 廃止 | ✅ | 計画に反映 | | **Medium** | 新ピア接続時に現 config を push | Codex | 計画に反映 | | **Low** | AudioSession interruption ハンドリング | Codex | 注意点に追記 | 計画を更新します。

## 14:30 - 作業ログ（自動記録）
- Geminiレビューが不完全で途中で終わっています（log形式のみで結果本体がない）。しかし issue は作成された様子なのでGitHub側を確認します。

## 14:46 - 作業ログ（自動記録）
- ご指示をお待ちしています。次に進める項目を教えてください。

## 15:21 - 作業ログ（自動記録）
- .gitignoreにbuildとxcodeprojを追加します。

## 15:26 - 作業ログ（自動記録）
- 完了です。何か他にありますか？
